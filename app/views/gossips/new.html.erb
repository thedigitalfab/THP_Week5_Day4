<h1>Bienvenue sur la page Gossip</h1>
<div class="container card-body bg-light">
  <%= form_tag url_for(action: 'create'), method: "post" do %>
    <div class= "d-flex card-title justify-content-center"> 
    <h4>Titre :</h4>
    <form action="/gossips" method="POST">
    <input type="text" name="title" size="100">
    </div>
    <div class= "card-body"> 
    <p>Saisis le contenu du potin ici :</p>
    <form action="/gossips" method="POST">
    <input class="inputfield" type="text" name="content" size="100"></br>
    <input type="submit" value="Envoyer le formulaire">
    </div>
  </form>
  <% if @gossip != nil %>
  <% if @gossip.errors.any? %>
    <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <ul>
      <% @gossip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  <%end%>
  <%end%>
  </div>